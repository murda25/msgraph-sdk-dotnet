namespace Microsoft.Graph.Models {
    public enum EducationSubmissionStatus {
        Working,
        Submitted,
        Released,
        Returned,
        UnknownFutureValue,
        Reassigned,
    }
}
